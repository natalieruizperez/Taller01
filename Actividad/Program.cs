namespace Actividad
{
    //Hay que implementar el método "NumerosPares" que recorra el arreglo "numeros" e imprima solo los números pares.
    internal class Program
    {
        static void Main(string[] args)
        {
             
           int[] numeros = { 2, 7, 14, 9, 10, 23, 18 };             

            
        }

        static void NumerosPares()
        {
            Console.WriteLine("Numeros pares:");

            for (int i = 0;  i < lista.Length; i++)
            {
                if ()
                {
                    Console.Write("Es numero par:" + );
                }

            }
        }
    }

}

